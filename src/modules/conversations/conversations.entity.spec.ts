import { ConversationsEntity } from './conversations.entity';

describe('ConversationsEntity', () => {
  it('should be defined', () => {
    expect(new ConversationsEntity()).toBeDefined();
  });
});
