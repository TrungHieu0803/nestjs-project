import { ConversationReplyEntity } from './conversation-reply.entity';

describe('ConversationReplyEntity', () => {
  it('should be defined', () => {
    expect(new ConversationReplyEntity()).toBeDefined();
  });
});
